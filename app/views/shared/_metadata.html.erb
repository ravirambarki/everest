<% if content_for?(:metadata) %>
  <%= yield :metadata %>
<% else %>
  <meta name="keywords"     content="<%= meta_keywords_default %>">
  <meta name="description"  content="<%= meta_description_default %>">
<% end %>
