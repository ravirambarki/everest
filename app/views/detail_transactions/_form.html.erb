<%= form_for(@detail_transaction) do |f| %>
  <% if @detail_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detail_transaction.errors.count, "error") %> prohibited this detail_transaction from being saved:</h2>

      <ul>
      <% @detail_transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <table>
  	<thead>
  	<tr>
      <td class="trans-label span2"><%= f.label "Product" %></td>
      <td class="trans-field span4">	
		<select id="sku" name="detail_transaction[sku]" class="selectpicker" data-style="btn-primary">
			<option value="0">SELECT PRODUCT</option>
			<% if !(@added_products.include? "AC ROOFING 3.60M") %>
    		<option value="AC ROOFING 3.60M">AC ROOFING 3.60M</option>
    		<%end%>
    		<% if !(@added_products.include? "AC ROOFING 3.00M") %>
    		<option value="AC ROOFING 3.00M">AC ROOFING 3.00M</option>
    		<%end%>
    		<% if !(@added_products.include? "AC ROOFING 2.75M") %>
    		<option value="AC ROOFING 2.75M">AC ROOFING 2.75M</option>
    		<%end%>
    		<% if !(@added_products.include? "AC ROOFING 2.50M") %>
    		<option value="AC ROOFING 2.50M">AC ROOFING 2.50M</option>
    		<%end%>
    		<% if !(@added_products.include? "AC ROOFING 2.25M") %>
    		<option value="AC ROOFING 2.25M">AC ROOFING 2.25M</option>
    		<%end%>
    		<% if !(@added_products.include? "AC ROOFING 2.00M") %>
    		<option value="AC ROOFING 2.00M">AC ROOFING 2.00M</option>
    		<%end%>
    		<% if !(@added_products.include? "AC ROOFING 1.75M") %>
    		<option value="AC ROOFING 1.75M">AC ROOFING 1.75M</option>
    		<%end%>
    		<% if !(@added_products.include? "AC ROOFING 1.50M") %>
    		<option value="AC ROOFING 1.50M">AC ROOFING 1.50M</option>
    		<%end%>
    		<option value="OTHERS">OTHERS</option>
		</select>
      </td>
  	</tr>
  	</thead>
   </table>
   

   <div id="showMe">
   <table>
  	<thead>
	<tr>
      <td class="trans-label span2"><%= f.label :openingStock, 'Opening Stock' %></td>
      <td class="trans-field span4"><%= f.text_field :openingStock %></td>
  	</tr>
  	<tr>
      <td class="trans-label span2"><%= f.label :purchaseWeekStock, 'Purchase Week Stock' %></td>
      <td class="trans-field span4"><%= f.text_field :purchaseWeekStock %></td>
  	</tr>
  	<tr>
      <td class="trans-label span2"><%= f.label :salesWeekStock, 'Sales Week Stock' %></td>
      <td class="trans-field span4"><%= f.text_field :salesWeekStock %></td>
  	</tr>
  	<tr>
      <td class="trans-label span2"><%= f.label :estimatedSelling, 'Est. Selling Price (in Running Mts)' %></td>
      <td class="trans-field span4"><%= f.text_field :estimatedSelling %></td>
  	</tr>
  </thead>
   </table>
   
   
   	<%= f.hidden_field :dealership_code, :value => @user.dealership_code%>
  	<div class="actions span6">
  		<%= f.submit "Add Product" , class: "btn span6 btn-info"%>
    	<%= button_to 'Submit',root_url, :class => 'btn span6 btn-info' %>
  	</div>
  </div>
<% end %>


<script>
$("#sku").on("change", function() {
	$("#showMe").show();
    if($(this).val() == "0"){
    	$("#showMe").hide()
    }
    else
    {
    	$("#showMe").show()
    }
})
</script>
